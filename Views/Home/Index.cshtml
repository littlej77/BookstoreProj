
@{
    ViewData["Title"] = "Bookstore Home Page";
}
@model BooksViewModel

@foreach (Book b in Model.Books)
{
    //coding by convention -- it will go look for booksummary in the shared folder
    //the model part sends the b over to the partial view (BookSummary)
    <partial name="BookSummary" model="b"/>
}
<div page-blah="@Model.PageInfo" page-action="Index" page-class="btn" page-classes-enabled="true"
        page-class-normal="btn-outline-dark" page-class-selected="btn-primary"></div>


         
         
 
 @*<a asp-action="Index" asp-route-pageNum="1">1</a>
<a asp-action="Index" asp-route-pageNum="2">2</a>
<a asp-action="Index" asp-route-pageNum="3">3</a>*@
 @*when you see Page-blah go use the special tag helper i set up (paginationTagHelper) and you are going to pass
                it in all the PageInfo that came in the Model from the Controller.*@


